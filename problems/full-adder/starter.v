module full_adder (
    input  wire a,
    input  wire b,
    input  wire cin,
    output wire sum,
    output wire cout
);
    // TODO: drive sum and cout for a + b + cin
    assign sum  = 1'b0;
    assign cout = 1'b0;
endmodule
